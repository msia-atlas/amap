{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ContratProducteur list</h1>

    <table class="table table-striped" id="tableauGestion">
        <thead>
            <tr>
                <th>Producteur</th>
                <th>Quantiteaproduire</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('contratProducteur_show', { 'id': entity.id }) }}">{{ entity.producteur }}</a></td>
                <td>{{ entity.quantiteAProduire }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('contratProducteur_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('contratProducteur_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('contratProducteur_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
