{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Amap list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Adresse</th>
                <th>Nom</th>
                <th>Responsable</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('amap_show', { 'id': entity.id }) }}">{{ entity.adresse }}</a></td>
                <td>{{ entity.nom }}</td>
                <td>{{ entity.responsable }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('amap_show', { 'id': entity.id }) }}">show</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {% endblock %}
