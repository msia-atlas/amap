{% extends 'AmapBundle:Default:layout.html.twig' %}

{% block main -%}
    <h1>Amap</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Adresse</th>
                <td>{{ entity.adresse }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ entity.nom }}</td>
            </tr>
            <tr>
                <th>Responsable</th>
                <td>{{ responsable? responsable.nom : "" }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('amap') }}">
            Retour à la liste des amaps
        </a>
    </li>
    <li>
        <a href="{{ path('amap_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
