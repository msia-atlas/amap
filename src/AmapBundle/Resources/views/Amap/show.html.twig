{% extends 'AmapBundle:Default:layout.html.twig' %}

{% block main -%}
<div class="row">
    <div class ="col-md-12 titre">
         <h1>AMAP</h1>
    </div>
</div>
<div class="row">
    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Adresse</th>
                <td>{{ entity.adresse }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ entity.nom }}</td>
            </tr>
            <tr>
                <th>Responsable</th>
                <td>{{ responsable? responsable.nom : "" }}</td>
            </tr>
        </tbody>
    </table>
</div>
<div class="row">
        <div class ="col-md-1">
            <a href="{{ path('amap_edit', { 'id': entity.id }) }}"  class="btn btn-default" role="button">
                Modifier
            </a>
        </div>
        <div class ="col-md-1">   
            {{ form(delete_form) }}
        </div>    
    </div>
        

    <div class="row">
        <div class="record_actions">
            <ul>
                <li>
                    <a href="{{ path('amap') }}">
                        Retourner à la liste des AMAPS
                    </a>
                </li>
            </ul>
        </div>
    </div>
{% endblock %}
