{% extends 'AmapBundle:Default:layout.html.twig' %}

{% block main -%}
    <h1>PreferenceConsommateur</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Produit</th>
                <td>{{ entity.produit }}</td>
            </tr>
            <tr>
                <th>Preference</th>
                <td>{{ entity.preference }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('preferenceConsommateur') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('preferenceConsommateur_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
