
<div class="header ">
    <nav class="navbar navbar-inverse  navbar-fixed-top">
         <div class="navbar-header">
            <div class="container-fluid">
                <div class="navbar-header">
                    <a class="navbar-brand" href="#"><img src="{{ asset('bundles/amap/images/logo.jpg') }}" /></a>                 
                </div> 
                <ul class="nav navbar-nav">
                    <li><a href="/">Accueil</a></li>   
                    {% if is_granted("ROLE_DROIT_ACCES_RESPONSABLE") %}
                        <li><a href="{{path("magasin")}}">Gestion des magasins</a></li>
                        <li><a href="{{path("contratConsommateur")}}">Gestion des contrats Consommateurs</a></li>
                        <li><a href="{{path("enConstruction")}}" >Gestion des contrats Producteur</a></li>
                        <li><a href="{{path("panierType")}}">Gestion des paniers types</a></li>
                    {%endif%}
                    {% if is_granted("ROLE_ADMIN") %}
                        <li><a href="{{path("amap")}}" >Gestion des Amaps</a></li>
                        <li><a href="{{path("produit")}}" >Gestion des Produits</a></li>
                        <li><a href="{{path("responsable")}}">Gestion des responsables</a></li>
                        <li><a href="{{path("saison")}}">Gestion des Saison</a></li>
                    {%endif%}
                    {% if is_granted("ROLE_DROIT_ACCES_CONSOMMATEUR") %}
                    <li><a href="{{path("contratConsommateur")}}">Mes contrats</a></li>
                    <li><a href="{{path("preferenceConsommateur")}}">Gestion de mes préférences</a></li>
                    {%endif%}
                    {% if is_granted("ROLE_DROIT_ACCES_PRODUCTEUR") %}
                    <li><a href="{{path("enConstruction")}}">Mes contrats</a></li>
                    <li><a href="{{path("production")}}">Gestion de mes productions</a></li>
                    {%endif%}
                </ul>
                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    <ul class="nav navbar-nav">
                        <li>
                            <a href="{{ path('fos_user_profile_show') }}"><span class="glyphicon glyphicon-user" aria-hidden="true"></span> Bonjour {{ app.user.username }}</a>
                        </li>
                        <li>                           
                            <a href="{{ path('fos_user_security_logout') }}"> <span class="glyphicon glyphicon-off" aria-hidden="true"></span> Déconnexion</a>
                        </li>
                    </ul>
                {% else %}
                    <ul  class="nav navbar-nav navbar-right">
                        <li><a href="{{ path('fos_user_security_login') }}"><span class="glyphicon glyphicon-user" aria-hidden="true"></span> Connexion</a></li>
                        <li><a href="{{ path('fos_user_registration_register') }}"><span class="glyphicon glyphicon-log-in"></span> Inscription</a></li>
                    </ul>
                {% endif %}
            </div>
         </div>
    </nav>
</div>