{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ContratConsommateur</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Consommateur</th>
                <td>{{ entity.consommateur }}</td>
            </tr>
            <tr>
                <th>Listelivraison</th>
                <td>{{ entity.listeLivraison }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('contratconsommateur') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('contratconsommateur_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
