{% extends 'AmapBundle:Default:layout.html.twig' %}
{% block main -%}
<div class="row">
    <div class ="col-md-12 titre">
        <h1>Gestion des contrats consomateurs</h1>
    </div>
</div>
<div class="row">
    <table class="table" id="tableauGestion">
        <thead>
            <tr>
                <th>Saison</th>
                <th>Statut</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('contratConsommateur_show', { 'id': entity.id }) }}">{{ entity.saison.libelle }}</a></td>
                <td>{{ entity.statut }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('contratConsommateur_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('contratConsommateur_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
<div class="row">
    <a href="{{ path('contratConsommateur_new') }}" class="btn btn-success pull-right" role="button">
        Nouveau contrat
    </a>
</div>
{% endblock %}

